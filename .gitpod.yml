tasks:
  - name: Dev Server
    init: |
      git config core.fileMode false
      chmod -R 777 .
      cd gitpod
      docker-compose up --no-start
    command: |
      cd gitpod
      docker-compose up

ports:
  - port: 8080
