tasks:
  - name: Dev Server
    init: |
      cd gitpod
      docker-compose up --no-start
    command: |
      git config core.fileMode false
      chmod -R 777 .
      cd gitpod
      docker-compose up

# TODO:

# Backend dev setup:
# - run make composer

# Frontend dev setup:
# - run make dev-setup
# - run make watch-js

ports:
  - port: 8080
